import { ChangeProductCardDirective } from './change-product-card.directive';

describe('ChangeProductCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeProductCardDirective();
    expect(directive).toBeTruthy();
  });
});
